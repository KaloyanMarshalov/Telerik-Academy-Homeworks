using System;

//Describe the strings in C#.
//What is typical for the string data type?
//Describe the most important methods of the String class.

namespace _01.StringsInCSharp
{
    class Strings
    {
        static void Main()
        {
            /*
             * Strings are a sequence (array) of Unicode symbols. They are read only, meaning you cannot 
             * put a character on a certain position in the string. They are stored in the dynamic memory 
             * (managed heap). Strings have fixed length and can be accessed by index. 
             * 
             * Most important methods:
             * IndexOf, LastIndexOf;
             * Replace, Remove;
             * ToUpper, ToLower;
             * Trim(chars);
             * 
             * */
        }
    }
}
